#!/usr/bin/env ruby
# frozen_string_literal: true

# Fixture reusable maintenance script for YiffSpace::FixTrackerTest - not numbered, so it must be
# excluded from FixTracker.all_fixes/pending and only reachable via `fixes:run[name]`.
