name: Publish yiffspace-core
permissions:
  contents: read
  id-token: write
on:
  workflow_dispatch:
  release:
    types: [ released ]

jobs:
  rubocop:
    uses: ./.github/workflows/rubocop.yml
  zeitwerk:
    uses: ./.github/workflows/zeitwerk.yml
  publish:
    if: github.event_name == 'workflow_dispatch' || startsWith(github.event.release.tag_name, 'yiffspace-core-v')
    needs: [ rubocop, zeitwerk ]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: yiffspace-core/.ruby-version
          bundler-cache: true
          working-directory: yiffspace-core
      - uses: rubygems/release-gem@v1
        with:
          working-directory: yiffspace-core
