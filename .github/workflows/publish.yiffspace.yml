name: Publish yiffspace
permissions:
  contents: read
  id-token: write
on:
  workflow_dispatch:
  release:
    types: [ released ]

jobs:
  rubocop:
    if: github.event_name == 'workflow_dispatch' || startsWith(github.event.release.tag_name, 'yiffspace-v')
    uses: ./.github/workflows/rubocop.yml
  zeitwerk:
    if: github.event_name == 'workflow_dispatch' || startsWith(github.event.release.tag_name, 'yiffspace-v')
    uses: ./.github/workflows/zeitwerk.yml
  publish:
    if: github.event_name == 'workflow_dispatch' || startsWith(github.event.release.tag_name, 'yiffspace-v')
    needs: [ rubocop, zeitwerk ]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: .ruby-version
          bundler-cache: true
          working-directory: packages/yiffspace
      - uses: rubygems/release-gem@v1
        with:
          working-directory: packages/yiffspace
