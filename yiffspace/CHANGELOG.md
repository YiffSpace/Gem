## 0.2.0

- Became a husk gem that depends on every `yiffspace-*` gem instead of
  containing code directly - see the individual gems' own CHANGELOGs
  (particularly `yiffspace-core`'s) for what moved where.
